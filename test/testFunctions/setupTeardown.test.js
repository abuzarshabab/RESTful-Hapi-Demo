// const { number } = require("joi");

// beforeEach(() => {
//     return initializeUserDatabase();
// })

// test('User database should have Abuzar', () => {
//     expect(isUser('Abuzar')).toBeTruthy()
// })

// //Scoped
// describe('Country to flag', () => {
//     beforeEach
//     afterEach
//     beforeAll
//     afterAll
// })

test.only('it should pass', () => {
    const c = 2;
    expect(c).toBe(2);
})